#include "vector.h"
#include "matrix.h"

///To check result is converge or not.
///return 1 if true
///return 0 if false
int converge(  ){

    int i;

    for( i = 0 ; i < ; i++ ){

        if( true )
            return 0;

    }

    return 1

}

void matrix_solution( VEC emittion, VEC b, VEC refection, MAT FF ){

    VEC bx = vCreate( b.elements );
    int k, x;

    while( converge( bx ) ){

        for(  )








    }

}
